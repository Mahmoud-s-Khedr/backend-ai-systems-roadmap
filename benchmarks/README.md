# Benchmarks

Store reproducible performance investigations here.

Always record the environment, dataset/load shape, tool versions, baseline, hypothesis, change, results and trade-offs. Prefer p50/p95/p99 latency, throughput, error rate, CPU/memory, DB timing and AI token/cost measurements where relevant.

Use `templates/benchmark-report.md` as the starting point.

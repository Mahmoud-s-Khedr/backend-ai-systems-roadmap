# Observability

Store SLOs, dashboards, alert definitions, traces/metrics investigations and telemetry architecture here.

Evidence should answer operational questions such as:

- Is the service meeting its SLO?
- Which dependency causes tail latency?
- What changed after a deployment?
- Can one request be traced across service boundaries?
- Are alerts actionable rather than noisy?
